import 'package:flutter/material.dart';

class DonationOptionsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Donation Options'),
      ),
      body: Center(
        child: Text('Options for donations here.'),
      ),
    );
  }
}
